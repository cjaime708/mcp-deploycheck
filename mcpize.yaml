# MCPize Configuration
# Docs: https://mcpize.com/docs/mcpize-yaml

version: 1

name: "DeployCheck"
slug: "mcp-deploycheck"

runtime: typescript

build:
  install: "npm ci"

start:
  type: stdio
  command: "node src/index.mjs"
